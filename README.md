After having finished [Make-A-Lisp](https://github.com/kanaka/mal) in Python 3, I decided to move to trying it out in Rust since I am learning the language. I will again try to avoid as many external inputs as necessary, unless it is genuinely trivial like `rustyline` for collecting user input in a nice way. It has no bearing on the program's functionality itself, so I don't mind it.
